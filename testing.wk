# This example is not guaranteed to work.

list := [for i:=1 \ i<=5 \ i:=i+1 do (i-1)];

for el in *list do (
	write (el, ', ')
);
write '\n';

a := 2;
b := 4;
c := 6;
d := 8;
e := 10;
(for el in *list do (list[el])) := 0+2,0;
write "\n";
write list;
write '\n'
