# This example is not guaranteed to work.

list := [for i:=1 \ i<=5 \ i:=i+1 do (2*i)];

for el in *list do (
	write (el, ', ')
);
write '\n';

a := 2;
b := 4;
c := 6;
d := 8;
e := 10;

for i:=4 \ i >= 0 \ i:=i-1 do list[i] := 0;

for el in *list do (
	write (el, ', ')
);
write '\n'

